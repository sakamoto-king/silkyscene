## 运行时对象与状态模型

SilkyScene 的核心是一套清晰的对象模型，其中 Element、Scene、Renderer 各司其职。理解这个模型对于正确使用 SilkyScene 至关重要。

### 生命周期与所有权

**Element** 的生命周期由 Presentation（演示文稿）管理。一个 Element 一旦被加入 Presentation，就会存在于整个演示的生命周期内。Element 可以在多个 Scene 之间使用，其状态在不同 Scene 中可以不同。

**Scene** 是状态的集合，不拥有 Element。每个 Scene 本质上是一张"元素状态快照"，记录某一时刻每个 Element 应该如何表现。Scene 与 Scene 之间可以共享同一批 Element，只要它们分别为这些 Element 定义不同的状态。

**Presentation** 是容器与编排器。它保持 Element 池、管理 Scene 列表、驱动场景切换与时间线更新。一个 Presentation 对应一个演示实例，可能展示在网页某个容器中。

Presentation 支持配置全局默认场景切换动画（`sceneTransition`），Scene 也支持配置自己的切换动画。语义是：**切换到某个 Scene 时，优先使用该 Scene 的动画配置；未配置字段回落到 Presentation 默认值**。

### Element 的数据结构

Element 是纯数据对象，包含八层属性：

1. **基础信息**：id（系统自动分配、只读、唯一标识）、name（易读名称）、type（类型标签）、visible（可见性）、locked（锁定状态）
2. **层级结构**：parent（父元素引用）、children（子元素数组），支持嵌套和树形关系
3. **布局描述**：layout 对象，用相对坐标描述元素相对于父容器的位置与尺寸（支持 mode:"relative"|"absolute"|"flex"，left/top/width/height 可以是像素、百分比或 "auto"，anchorX/anchorY 定义旋转与缩放的锚点）
4. **变换数据**：transform 对象，用平台无关的格式描述缩放、旋转、平移（x、y、scaleX、scaleY、rotation），便于多平台渲染
5. **视觉属性**：opacity（透明度）、zIndex（堆叠顺序）、blendMode（混合模式）
6. **计算结果**：computed 对象，存放最终的绝对坐标与尺寸（x、y、width、height），由 Renderer 计算并赋值
7. **运行时状态**：dirty 标志（标记需要重新计算），mounted（已挂载标志），支持增量渲染优化
8. **渲染器引用**：Renderer 使用 WeakMap 等间接方式维护 Element 到 DOM 的映射，Element 本身不包含 DOM 字段

这样的分层设计使得 Element 可以独立定义，不依赖特定的渲染后端。DOM、Canvas、WebGL 都可以通过 Renderer 来消费这些属性。

ID 策略说明：

- Element 的 `id` 在创建时由框架自动生成。
- `id` 对外只读，可获取但不可修改。
- 业务层不再手动传入或维护 `id`。
- `Scene.name` 仍保留手动命名能力，因为它是语义标签，不是全局唯一键。

### Scene 的状态管理

Scene 通过 states 字典维护每个 Element 的状态快照。当调用 `scene.setState(element, { layout: { left: "50%" } })` 时，只有这个具体属性被记录，而 Element 本体不受影响。

多个 Scene 中的同一个 Element 可以有不同的状态：Scene A 让它居中，Scene B 让它靠左。当演示在 Scene 之间切换时，Renderer 会根据新的 Scene 状态重新计算 Element 的 computed，从而驱动动画或跳转。

Scene 还支持在状态声明层添加语义元数据（例如 `entrance`），用于描述"首次出现时如何构造过渡起点"。这类语义只参与运行时解析，不会改写历史场景的状态表。

### 场景继承与显式移除

运行时会在 `Presentation` 侧预计算场景最终态缓存（resolved cache）：

1. 当前场景未声明的元素，默认继承上一场景最终态。
2. 当前场景 `setState` 会覆盖继承结果。
3. 当前场景 `removeState` 会写入显式移除标记（tombstone），阻断继承。

这意味着业务层不再需要在每个场景重复声明所有元素。

示例：

```js
const s1 = new Scene("s1")
const s2 = new Scene("s2")
const s3 = new Scene("s3")

s1.setState(title, { opacity: 1 })
// s2 未声明 title，仍会继承可见

s3.removeState(title)
// s3 开始 title 被显式移除，不再继承
```

示例：

```js
scene2.setState(title, {
	layout: { left: "50%", top: "40%" },
	opacity: 1,
}, {
	entrance: true,
})
```

上例中，如果 `title` 在前一场景没有状态，运行时会自动补一个前态；默认规则是 `opacity: 0`。

### 计算流程

当 Scene 发生变化时（通常是用户点击或自动播放切到新场景），运行时执行以下流程：

1. **语义解析与状态补全（Presentation）**：
	- Presentation 侧维护 resolved cache（跨场景继承 + tombstone），切场景前确保缓存可用。
	- 解析本次切场景的 transition 配置（duration/easing），写入容器样式与 CSS 变量。

2. **生成切场景执行计划（transitionPlan / SceneChangePlan，Presentation）**：
	- 将“方向（forward/backward）”“entrance/exit 的 from/to 解析”“delay 镜像”等语义，生成一份纯数据 plan。
	- plan 的每个 item 会包含本次切换该元素需要的 fromState/toState、是否入场（isEntering）、最终 delay（finalDelay）等。
	- 该 plan 必须是纯数据（不含 DOM 引用），便于测试与复用。

3. **执行渲染计划（Renderer）**：
	- Renderer 消费 plan：创建/复用节点、写入 delay、应用 from/to 状态。
	- 对“入场元素”采用两帧机制：Phase 1 写 fromState（禁用过渡）→ reflow → Phase 2 在 rAF 写 toState（恢复过渡）。
	- 布局换算由 Layout.resolve 完成（百分比到像素），最终通过 transform/opacity 写回 DOM。

补充说明：
- 业务侧通常不需要直接构造或传入 transitionPlan；推荐始终通过 `Presentation.setScene()` / `nextScene()` / `prevScene()` 切场景。
- 仅当你在做自定义渲染后端，或显式绕过 setScene 链路时，才需要关心 transitionPlan 的生成与传递。

### 响应式尺寸规范（第一版）

为了避免像素尺寸在响应式容器里失真，尺寸字段第一版统一规则如下：

1. 尺寸基准为 `min(contentWidth, contentHeight)`（容器短边）。
2. 尺寸字段只接受百分比字符串（如 `"2.4%"`）。
3. 像素字符串与裸数字像素不再接受（会抛错）。

受该规范约束的常见字段：

- 文本：`fontSize`
- 线条：`line.strokeWidth`、`line.cornerRadius`
- 箭头：`arrow.size`、`arrow.width`
- 图形：`shape.strokeWidth`、`shape.cornerRadius`
- 方向动画：`entrance.distance`、`exit.distance`

说明：

- 坐标字段（如 `x1/y1/x2/y2`、`layout.left/top`）仍可继续使用百分比或数值坐标。
- `transform.rotation` 继续使用弧度。
- `transform.x/y` 支持百分比偏移；数值偏移当前保留兼容。

### 图形元素状态扩展

矩形继续使用 `ShapeElement`，通过 `shapeType: "rect"` 表达。推荐将图形样式覆盖写在 `sceneState.shape` 下：

```js
scene.setState(cardRect, {
	opacity: 1,
	transform: { x: "8%", y: "6%", rotation: 0.2 },
	shape: {
		fill: "#ff7aa2",
		stroke: "#fff1c9",
		strokeWidth: "0.55%",
		cornerRadius: "3.6%",
	},
})
```

其中：

- `shape.fill`：填充颜色
- `shape.stroke`：描边颜色
- `shape.strokeWidth`：描边宽度（百分比）
- `shape.cornerRadius`：圆角半径（百分比）

这些字段与 `transform/opacity` 一起参与场景切换和继承解析。

`ShapeElement` 当前重点支持两种图形语义：

- `shapeType: "rect"`：使用 `shape.cornerRadius` 控制圆角
- `shapeType: "circle"`：忽略 `shape.cornerRadius`，直接使用最大圆角；当宽高不等时允许视觉上呈现为椭圆外观

当前 Presentation 已提供 `resolveSceneTransition(fromScene, toScene)` 与 `resolveElementTransition(element, fromScene, toScene)` 两个入口：

- 可直接拿到 `fromState/toState` 与切换方向（`forward/backward`）
- 内置 entrance 语义解析
- 正放与倒放保持同一套规则

切场景时，运行时会先解析目标 Scene 的动画配置（时长 + 贝塞尔曲线），然后写入 `Presentation.content` 的动画样式（含 CSS 变量），再触发 `onSceneChanged`。这样 Renderer 与业务层都可以读取同一份生效配置。

当前默认实现中，`Presentation` 会在 `start()` 时初始化并接入 `Renderer`，同时自动绑定键盘和触摸翻页交互。在不覆写 `onSceneChanged` 的情况下，场景切换会自动触发默认 DOM 渲染：

- 为每个 Element 建立/复用节点
- 读取场景状态并解析布局
- 仅更新 `transform` 与 `opacity`

这个流程是**同步的、按需的、确定性的**。不会在帧循环中反复执行，也不会因为媒体播放或其他异步事件隐式触发。

### 多个 Renderer 与多平台支持

虽然当前主要使用 DOM Renderer，但架构从一开始就为多平台准备。Canvas Renderer、WebGL Renderer 甚至自定义渲染后端都只需要：

1. 理解 Element 的 computed 与 transform 数据格式
2. 拦截 Scene 换场的触发点，重新计算布局
3. 在帧循环中按照现有 transform 进行插值与渲染

这样，同一批 Element 就可以在不同渲染后端间无缝切换。

### 与传统 MVC 的类比

如果把 SilkyScene 看作数据管理模式，可以类比为：

- **Model**：Element（存放数据）
- **View State**：Scene（存放作用于某个 View 的状态）
- **Controller**：Presentation（协调生命周期与场景切换）
- **View**：Renderer（根据 Model + View State 生成输出）

这种分离使得单个 Element 可以在多个 Scene（多个 View State）中使用，而无需复制对象。

### Presentation 生命周期 API

| 方法 | 说明 |
| --- | --- |
| `start()` | 挂载到容器，初始化渲染器，绑定交互（键盘、触摸、尺寸监听） |
| `stop()` | 解绑所有交互，卸载渲染器，移除 DOM 节点，释放资源 |

### 内置导航交互

`Presentation` 在 `start()` 时会自动绑定以下交互，无需业务层手动处理：

**键盘**（可通过 `navigation.keys.enabled: false` 关闭）

| 按键 | 行为 |
| --- | --- |
| `ArrowRight` / `ArrowDown` / `PageDown` / `Enter` / `Space` | 下一场景 |
| `ArrowLeft` / `ArrowUp` / `PageUp` | 上一场景 |

当焦点在 `input`、`textarea`、`select` 或 `contenteditable` 元素上时，键盘事件自动忽略。

**触摸滑动**（可通过 `navigation.touch.enabled: false` 关闭）

| 手势 | 行为 |
| --- | --- |
| 左滑 / 上滑 | 下一场景 |
| 右滑 / 下滑 | 上一场景 |

默认最小触发距离为 50px，可通过 `navigation.touch.threshold` 调整。

```js
const app = new Presentation(container, {
	navigation: {
		loop: false,
		keys: { enabled: true },
		touch: { enabled: true, threshold: 50 },
	},
})
```
